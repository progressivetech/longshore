--- js/webform.js.orig
+++ js/webform.js
@@ -696,7 +696,7 @@
         $this.webformProp('selected', on ? value : false);
       }
       else {
-        $this.val(on ? [value] : false);
+        $this.val(on ? [value] : []);
       }
     });
     return this;
